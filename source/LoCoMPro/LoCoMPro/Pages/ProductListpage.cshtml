@page
@model LoCoMPro.Pages.ProductListPageModel
@{
    ViewData["Title"] = "Lista de Productos";
    Layout = "_Layout";
    ViewBag.ShowSearchBar = true;
    ViewBag.ShowLogoLink = true;
}

<head>
    <link rel="stylesheet" href="~/css/ProductListPage.css" />
</head>

<div>

    <div class="information-section">
        <!-- Title of the list-->
        <div class="list-page-title">
            <h2>Lista de productos de interés</h2>
        </div>

        <!-- Amount of products in the list-->
        <div class="list-page-subtitle">
            <h4>Cantidad de productos:</h4>
            <!-- Something here -->

            <h4>Precio aproximado:</h4>
            <!-- Something here -->
        </div>
    </div>

    <!-- Horizontal separating line -->
    <hr class="style-four">

    <div class ="register-list-section">
        <!-- Container for column -->
        <nav>
            <ul class="list-page-column-title">
                <li>
                    <div class="textComment">Producto</div>
                </li>
                <li>
                    <div class="textComment">Marca</div>
                </li>
                <li>
                    <div class="textComment">Modelo</div>
                </li>
                <li>
                    <div class="textComment">Tienda</div>
                </li>
                <li>
                    <div class="textComment">Ubicación</div>
                </li>
                <li>
                    <div class="textComment">Precio Promedio</div>
                </li>
                <li>
                </li>
            </ul>
        </nav>

        <!-- Search results section -->
        <ul class="register-list-section">
            <!-- Put in all results -->
            @* @foreach (var register in Model.RegisterList)
            {
                <!-- A result block -->
                <li class="register-list-block register-block-pagination">
                    <!-- Route and data for each result -->
                    <a>
                        <span id="register-product-name" value="@register.ProductName">
                            @Html.DisplayFor(modelItem => register.ProductName)
                        </span>
                        <span id="register-store-name" value="@register.StoreName">
                            @Html.DisplayFor(modelItem => register.StoreName)
                        </span>
                        <span id="register-province-name" value="@register.ProvinciaName">
                            @Html.DisplayFor(modelItem => register.ProvinciaName)
                        </span>
                        <span id="register-canton-name" value="@register.CantonName">
                            @Html.DisplayFor(modelItem => register.CantonName)
                        </span>
                        <span id="register-price" value="@register.Price">
                            ₡ @Html.DisplayFor(modelItem => register.Price)
                        </span>
                        <span id="register-date" value="@register.SubmitionDate.Date">
                            @register.SubmitionDate.ToShortDateString()
                        </span>
                        <span id="register-categories" value="@Model.CategoryMap[register.Product.Name]" hidden></span>
                    </a>
                </li>
            } *@
        </ul>
        <!-- Pagination section -->
        @await Html.PartialAsync("_Pagination")
    </div>
    <script src="~/js/pagination.js"></script>
</div>